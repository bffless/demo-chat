name: PR Chat Demo Preview

on:
  pull_request:
    branches: ['*']

permissions:
  contents: read
  pull-requests: write

jobs:
  build-and-preview:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build Chat Demo
        run: pnpm build

      - name: Upload build preview
        uses: bffless/upload-artifact@v1
        with:
          path: dist
          api-url: ${{ vars.ASSET_HOST_URL }}
          api-key: ${{ secrets.ASSET_HOST_KEY }}
          alias: preview
          base-path: /dist
          description: 'Chat Demo preview for PR #${{ github.event.pull_request.number }}'
          summary-title: Chat Demo Preview Summary

      - name: Upload skills preview
        uses: bffless/upload-artifact@v1
        with:
          path: .bffless
          api-url: ${{ vars.ASSET_HOST_URL }}
          api-key: ${{ secrets.ASSET_HOST_KEY }}
          alias: preview
          description: 'Skills preview for PR #${{ github.event.pull_request.number }}'
          summary-title: Skills Preview
